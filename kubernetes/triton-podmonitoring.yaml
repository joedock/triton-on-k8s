apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: triton-inference-server
  namespace: triton
  labels:
    app: triton
spec:
  selector:
    matchLabels:
      app: triton
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
